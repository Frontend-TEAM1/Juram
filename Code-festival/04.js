// 변수의 타입2
// 다음 변수 a를 typeof(a)로 넣었을 때 출력될 값과의 연결이 알맞지 않은 것은?

let a1 = 1;         // number
let a2 = 2.22;      // boolean
let a3 = 'p';       // string
let a4 = [1,2,3];   // object

console.log(typeof(a2));

// 예상 답 : 2번 boolean이 아니라 double
// 실제 답 : 2번 하지만 double도 아닌 number

